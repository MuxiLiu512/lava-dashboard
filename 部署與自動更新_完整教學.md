# Lava Living Dashboard — 部署與自動更新 完整教學

寫給「只部署過一次」的你。全程可不碰終端機。讀完你會懂：① GitHub 到底幫你做什麼 ② 三支 API key 怎麼拿 ③ 自動更新怎麼運作。

---

## 0. 先搞懂三個角色（最重要）

| 角色 | 在哪裡跑 | 你要做的事 |
|---|---|---|
| **Repo（倉庫）** | GitHub 上 | 把 `lava-live` 的檔案放進去（一次性，可用網頁拖拉）|
| **GitHub Pages（部署/託管）** | **GitHub 的伺服器** | 開一個開關，它就 24h 幫你把網站掛在網路上 |
| **GitHub Actions（自動更新）** | **GitHub 的伺服器** | 設好 4 個密鑰，它每 3 小時自動抓資料更新 |

**直接回答你的問題：**
- 「能不能在主機端部署？」→ **可以，而且本來就是。** GitHub Pages 就是 GitHub 的主機在幫你部署、託管、上網。你的電腦**完全不需要開著**。
- 「還是只能透過主機端來推？」→ 把檔案放進 repo 有三種方式，**最簡單是用瀏覽器在 github.com 上傳（不用指令、不用本機 git）**。也可以用本機推（git 指令 或 GitHub Desktop App）。更新資料這件事則完全由 GitHub 伺服器端的 Action 做，不需要你推。

一句話：**部署與更新都在 GitHub 端自動發生；你只需要「把檔案放進去」這一次動作，而且可以全程在網頁完成。**

---

## 1. 把網站放上線（網頁版，~3 分鐘，免終端機）

1. 到 github.com 登入 →右上「+」→ **New repository**。
   - Name：`lava-dashboard`（隨意）
   - 選 **Public**（Pages 最簡單；之後我們用密碼遮蔽）
   - 按 **Create repository**。
2. 進到空 repo 頁 → 點 **「uploading an existing file」** 連結。
3. 打開你電腦的 `PM doc./lava-live` 資料夾，**把裡面的東西全選**（注意：是「裡面的內容」，不是整個 lava-live 資料夾）：
   `index.html`、`data.json`、`README.md`、`DESIGN_PHILOSOPHY.md`、`.gitignore`、`scripts/`、`.github/`
   直接**拖進** GitHub 的上傳框。
   - ⚠️ `.github` 資料夾（裡面是自動更新設定）有時拖拉會被忽略。若上傳後在 repo 看不到 `.github/workflows/refresh.yml`，見下方「補上 .github」。
4. 下方按 **Commit changes**。
5. 左上 repo 名稱旁確認 `index.html` 在**最外層**（根目錄），不是在 `lava-live/index.html` 裡。若被包了一層，重傳成「內容在根目錄」。

### 開啟 Pages（部署）
6. repo → **Settings**（上排）→ 左欄 **Pages**。
7. Source 選 **Deploy from a branch** → Branch 選 **main**、資料夾 **/(root)** → **Save**。
8. 等 1–2 分鐘，重新整理該頁，上方會出現綠色網址：
   `https://<你的帳號>.github.io/lava-dashboard/`
9. 打開它 → 輸入密碼 **`lava2026`** → 看到儀表板就成功了。
   （此時資料是內建種子值；做完第 3 步設密鑰後就會自動換成即時數字。）

### 補上 .github（若第 3 步沒成功）
- repo 首頁 → **Add file → Create new file** → 檔名欄輸入：
  `.github/workflows/refresh.yml`（打斜線會自動建資料夾）
- 把本機 `lava-live/.github/workflows/refresh.yml` 內容貼上 → Commit。
- `scripts/refresh_data.py` 同理（檔名輸入 `scripts/refresh_data.py` 再貼內容）。

---

## 2. 三支 API 是什麼、怎麼拿（給自動更新用）

自動更新腳本 `scripts/refresh_data.py` 會用「唯讀」的方式向兩個服務要數字。這些 key 存在 GitHub 的 **Secrets**（加密保險箱），**不會出現在公開網頁裡**，安全。

> 註：Google Drive / Calendar 這支 API **目前不被這個 Action 使用**（它在 CI 環境授權很麻煩）。會議記錄與行事曆的自動彙整，我會改用桌面版 Cowork 的排程來做（見 §4）。

### 2-1. PostHog（產品指標：WAU/MAU/配對/成交）
- **POSTHOG_API_KEY**：到 https://us.posthog.com → 右上頭像 → **Settings** → 左欄 **Personal API keys** → **Create personal API key**。
  - 名稱：`lava-dashboard`
  - Scopes（權限）：至少勾 **Query → Read**（保險起見可加 **Project → Read**）。
  - 建立後**立刻複製**那串 `phx_...`（只顯示一次）。
- **POSTHOG_PROJECT_ID**：就是 **`98614`**（也可在網址 `us.posthog.com/project/98614` 看到）。
- 腳本怎麼用它：對 `https://us.posthog.com/api/projects/98614/query/` 送一段 HogQL（類 SQL）查詢，例如「過去 7 天 `$screen` 的不重複用戶數」＝WAU。只讀不寫。

### 2-2. ClickUp（任務進度：完成/逾期/今日到期）
- **CLICKUP_API_TOKEN**：ClickUp → 左下角你的頭像 → **Settings** → 左欄 **Apps** → **API Token** → **Generate**（或 Copy），得到 `pk_...`。
- **CLICKUP_LIST_ID**：就是 **`901818809467`**（「🎯 CEO / PM / UI 追蹤」清單）。打開那個清單時網址會有這串。
- 腳本怎麼用它：對 `https://api.clickup.com/api/v2/list/901818809467/task` 做 GET，數一數各狀態與到期日。只讀。

### 2-3. 把密鑰放進 GitHub
- repo → **Settings** → 左欄 **Secrets and variables** → **Actions** → **New repository secret**，逐一新增：

| Name | Secret 值 |
|---|---|
| `POSTHOG_API_KEY` | 你的 `phx_...` |
| `POSTHOG_PROJECT_ID` | `98614` |
| `CLICKUP_API_TOKEN` | 你的 `pk_...` |
| `CLICKUP_LIST_ID` | `901818809467` |

---

## 3. 自動更新怎麼運作（操作細節）

檔案 `.github/workflows/refresh.yml` 定義了一個排程工作：

```
觸發：每 3 小時（cron "0 */3 * * *"，UTC 時間）／你也可手動／每次 push
動作：① 取出 repo ② 裝 Python ③ 跑 refresh_data.py（用上面的密鑰去要數字）
      ④ 如果 data.json 有變 → 自動 commit 並 push 回 repo
結果：Pages 重新部署，網頁上的「最後更新時間」就變新，綠色 LIVE 燈亮
```

**你可以這樣操作：**
- **手動跑一次測試**：repo → **Actions** 分頁 → 左邊點 **Refresh dashboard data** → 右邊 **Run workflow** → 綠勾代表成功。跑完去看網站，時間戳應更新。
- **改更新頻率**：編輯 `refresh.yml` 的 `cron`。例如每小時 `0 * * * *`；每天台灣早上 8 點（UTC 0 點）`0 0 * * *`。（cron 是 UTC，台灣 = UTC+8。）
- **頁面端**：開著的分頁每 60 秒自動重抓 `data.json`，不用手動 F5。
- **判斷是否 live**：頂部綠燈=資料 <26 小時；變黃「STALE」就去 Actions 看是不是失敗（多半是密鑰打錯）。

**第一次設完密鑰後**，記得到 Actions 手動 Run 一次，把種子資料換成真實數字。

---

## 4. 換密碼 & 之後的會議/頻道自動彙整

- **換密碼**：密碼用 SHA-256 雜湊存在 `index.html`（搜尋 `PW_HASH`）。Mac 終端機跑 `printf '新密碼' | shasum -a 256`，把 64 字元結果貼回去；或直接跟我說新密碼，我幫你算好給你貼。
- **會議記錄 + ClickUp 頻道自動抓任務**：這部分（你 #2 的需求）我會用桌面版 Cowork 的每日排程來做——它能讀 Google Drive 會議記錄與 ClickUp 頻道、用 AI 判別哪些是任務/bug/他部門，再寫進 `data.json`。這比 GitHub Action 適合（Action 不方便做 AI 判讀與 Google 授權）。細節等你回覆 §下面兩個選擇後我就建。

---

## 5. 不想用網頁上傳？兩個替代

- **GitHub Desktop（圖形介面，推薦給不熟指令的你）**：裝 App → File → Clone 你的 repo → 把 `lava-live` 內容複製進去 → 左下 Commit → 右上 Push。之後改檔也是這樣同步。
- **終端機 git**（你上次 BD 網頁可能用過）：
  ```bash
  cd lava-live
  git init && git add . && git commit -m "init"
  git branch -M main
  git remote add origin https://github.com/<帳號>/lava-dashboard.git
  git push -u origin main
  ```

---

## 6. 卡住時
- 網站 404 / 空白：等 2 分鐘；確認 `index.html` 在根目錄；確認 Pages 選 main /root。
- 數字沒更新：Actions 分頁看紅叉錯誤；多半是某個 Secret 名稱或值打錯（名稱要完全一致）。
- 看不到 .github：用 §1「補上 .github」手動建檔。
- 安全：API key 只放 Secrets，**永遠不要**寫進 index.html 或任何會被上傳的檔。
